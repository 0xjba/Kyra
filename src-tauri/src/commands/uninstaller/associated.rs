use super::AssociatedFile;

/// Finds files and directories associated with the given application.
pub fn find_associated(
    _bundle_id: &str,
    _app_name: &str,
    _app_path: &str,
) -> Vec<AssociatedFile> {
    vec![]
}
